#include "main.h"
#include <stdio.h>
/**
 * main - printes a program name
 * @argc: number of arguement
 * @argv: array of aguement
 *
 * Return: Always 0 (success)
 */
int main(int argc, char *argv[])
{
	(void)argc;

	printf("program name: %s\n", argv[0]);
	return (0);
}
